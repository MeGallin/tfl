import { SecondWordPipe } from './second-word.pipe';

describe('SecondWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondWordPipe();
    expect(pipe).toBeTruthy();
  });
});
